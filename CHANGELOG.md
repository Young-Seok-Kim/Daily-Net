# 변경 이력

버전을 올릴 때 이 파일 맨 위에 새 항목을 추가한다.
`app/build.gradle.kts` 에는 `versionCode` / `versionName` 만 두고, 상세 내역은 여기에 적는다.

각 버전은 두 부분으로 적는다.

- **스토어 문구** — Play Console "이 버전의 새로운 기능"에 그대로 붙여넣는 칸.
  일반 텍스트만 쓴다 (마크다운·굵게 안 먹힘). **500자 제한**. 사용자가 체감하는 것만.
- **상세 내역** — 개발용 기록. 스토어에는 올리지 않는다.

---

## v1.7.7 (b25) — 2026-07-29

### 스토어 문구

```
[개선]
• 오류가 생겼을 때 원인을 파악할 수 있도록 진단 기능을 추가했습니다.
```

### 상세 내역

- **권장 단백질 계산 오류 수정** (⚠️ 서버 배포로 즉시 반영됨)
  - 권장 칼로리의 **40%를 단백질로 배분**하고 있었다. 체중을 전혀 보지 않아
    2000kcal면 누구나 200g이 나왔고, 70kg 기준 2.9g/kg으로 웬만한 보디빌더보다 높았다
  - 단백질은 칼로리 비율이 아니라 **체중 기준**이 표준이다
    (일반 성인 0.8g/kg, 감량 중 근손실 방지 1.2~2.0g/kg → **1.6g/kg** 적용)
  - 단백질·지방을 먼저 정하고 **남은 칼로리를 탄수화물로** 채운다.
    그래야 세 값의 칼로리 합이 권장 칼로리와 맞는다
  - 지방 비율도 20% → 25%로 올렸다 (일반 권장 범위 20~35%의 하한에 붙어 있었다)
  - 70kg/175cm/30세 남성 예시: 탄 200→220g · 단 200→**112g** · 지 44→49g
  - 계산을 `functions/nutrition.js`로 분리해 **기준이 어디인지 명확히** 했다
  - ⚠️ 앱 `WeekSummary.kt`에 같은 계산이 있다. 한쪽만 고치면 리포트와 화면이 어긋난다
  - 이미 저장된 과거 리포트의 문구는 그대로다 (텍스트로 저장돼 소급 수정 불가)

- **주차 구분선을 누르면 그 주 요약이 펼쳐진다**
  - b24에서 끼니별 칼로리·탄단지를 저장하기 시작했는데 화면에서 쓰는 곳이 없었다.
    파이프만 깔고 물은 안 흘리던 상태를 처음으로 해소한 것
  - 새 화면을 만들지 않고 **이미 스크롤하며 지나가는 자리**에 붙였다
  - 하루 단위 요약은 만들지 않았다. AI 리포트 본문에 이미 끼니별 칼로리가 있어 중복이다.
    주 단위는 리포트가 알려주지 않는 정보라 여기서만 얻을 수 있다
  - 두 단으로 나눈다
    - 기록 일수·운동 일수·하루 평균 순칼로리 → `netCalories`는 예전부터 저장돼 **지난 주도 나온다**
    - 하루 평균 탄단지 → b24 이후 분석한 날만. 한 날도 없으면 이 구간을 통째로 숨긴다
  - **"(N일 기준)"을 반드시 함께 보여준다.** 7일 중 2일만 분석한 주의 평균을 그냥
    "하루 평균"이라 쓰면 한 주 전체로 오해한다
  - 권장 탄단지는 저장하지 않고 `bmr`에서 계산한다.
    ⚠️ `functions/analyzeDiet.js`와 같은 공식이라 한쪽을 고치면 다른 쪽도 고쳐야 한다
  - 여러 주를 동시에 펼칠 수 있다 (주끼리 비교하는 게 목적이라 아코디언은 오히려 불편)
  - 처음엔 텍스트 화살표(`▾`)를 썼는데 실기기에서 거의 안 보였다.
    → 강조색 아이콘으로 바꾸고 터치 영역도 넓혔다. 누를 수 있다는 신호가 없으면 없는 기능이다
  - 차트 라이브러리 없이 `Box` 비율 채우기로 그린다 (의존성 추가 없음)

- **Crashlytics 추가**
  - b24에서 Room 스키마·서버 인증·사진·알림·다국어를 한 번에 바꿨는데,
    정작 실사용자 기기에서 문제가 나도 알 방법이 없었다
  - 실제로 b24 개발 중 앱이 시작하자마자 죽는 문제가 있었고,
    그건 기기를 손에 들고 adb를 붙일 수 있어서 잡은 것이다. 실사용자였다면 그냥 앱을 지웠을 것이다
  - **삼킨 예외도 함께 보고한다** (`CrashReporter`)
    - 이 앱에서 잦은 실패는 앱이 죽는 게 아니라 조용히 실패하는 쪽이다
    - 분석 실패, 사진 인식 실패, `checkProfile` 실패를 비정상 종료가 아닌 형태로 올린다
    - 특히 `checkProfile` 실패는 화면상 "신규 사용자"와 구분이 안 돼서 반드시 필요했다
  - 한도 초과(429)는 정상 동작이라 보고하지 않는다

---

## v1.7.6 (b24) — 2026-07-29

### 스토어 문구

```
[새로운 기능]
• 음식 사진을 찍으면 메뉴를 자동으로 채워줍니다. 직접 타이핑할 필요가 줄어듭니다.
• 정산을 안 한 날 저녁에 알림을 받을 수 있습니다. (설정 > 알림)

[개선]
• 영어를 지원합니다. 기기 언어에 맞춰 화면과 AI 분석 리포트가 함께 바뀝니다.
• 나이를 반영해 기초대사량을 계산합니다. 분석 결과가 더 정확해집니다.
• 분석 결과의 끼니별 칼로리와 탄단지가 기록으로 남습니다.
• 꼭 필요한 업데이트가 있을 때 앱을 켜면 안내해 드립니다.
```

### 상세 내역

- **분석 결과를 구조화해서 저장** (⚠️ 서버 배포 필요)
  - 예전에는 리포트를 긴 문자열 하나로만 받아서, 화면에 뿌리는 것 말고는 할 수 있는 게 없었다.
    끼니별 칼로리도 탄단지도 텍스트에 묻혀 있어 차트도 기간별 집계도 불가능했고,
    매일 분석을 돌려도 쌓이는 건 `netCalories` 숫자 하나뿐이었다
  - 서버 응답에 `structured`를 **추가**했다. `feedback`은 그대로 두어 구버전 앱은 영향이 없다
  - Room에 끼니별 칼로리·탄단지·기초대사량을 컬럼으로 저장 (DB 7 → 8, `MIGRATION_7_8`)
  - b24 이전에 분석한 기록에는 이 값이 없다. 소급 생성이 불가능하므로
    `DailyRecordModel.hasNutritionData`로 걸러서 "데이터 없음"으로 다뤄야 한다

- **분석 횟수 검증을 서버로 이전** (⚠️ 서버 배포 필요)
  - 예전에는 앱이 횟수를 세고 Firestore에 썼다. 앱 데이터를 지우거나 문서를 손대면
    그대로 우회돼서, Gemini 호출 비용이 걸린 문제인데도 막을 방법이 없었다
  - 이제 요청에 Firebase ID 토큰을 실어 보내고, 서버가 트랜잭션으로 세고 차감한다
  - 분석이 실패하면 차감분을 되돌려준다 (실패한 분석까지 세면 사용자만 손해)
  - 날짜는 서울 기준으로 계산한다. 런타임이 UTC라 그냥 두면 한국 자정 이후 9시간 동안 어제로 센다
  - 앱의 사전 확인은 **막는 근거가 아니라 힌트**로만 쓴다
    - 로컬 횟수가 실제보다 클 수 있다 (환불된 분석, 다른 기기에서 쓴 값의 반영 지연)
    - 그 상태로 막으면 남은 횟수가 있는데 구독 창을 보게 되므로,
      막기 직전에 서버 값을 한 번 확인하고 로컬도 함께 맞춘다
    - 확인이 실패하면 **막지 않는다.** 진짜 초과면 서버가 429로 거절하므로,
      통신 장애 때 미리 막아버리는 쪽이 더 나쁜 실패다
    - 429 응답에 담긴 실제 횟수로 로컬을 갱신해 다음 사전 확인이 틀리지 않게 한다
  - `REQUIRE_AUTH = false`로 두어 토큰을 안 보내는 구버전은 예전처럼 통과시킨다.
    **min_version_code를 24로 올려 구버전을 정리한 뒤 true로 바꾸면 우회가 완전히 막힌다**

- **음식 사진으로 메뉴 채우기** (⚠️ 새 함수 `extractMeal` 배포 필요)
  - 식사 입력창의 📷 버튼 → 촬영 → 메뉴명이 입력창에 자동으로 채워진다
  - 결과를 확정하지 않고 **초안**으로 넣는다. AI의 양 추정은 정확하지 않아 사용자가 고쳐야 한다.
    이미 적어둔 내용은 지우지 않고 뒤에 덧붙인다
  - 사진은 **저장하지 않는다.** 768px로 줄여 보내고 텍스트로 바꾼 뒤 임시 파일을 지운다
    (Cloud Storage에 쌓으면 1인당 연 수백 MB인데 다시 볼 일이 거의 없다)
  - 분석 횟수를 소모하지 않고, 사진 인식만의 하루 상한을 따로 둔다
    (**무료 3회 / 구독 30회**, 서버 상수라 앱 배포 없이 조정 가능)
  - 무료도 쓸 수 있게 열어둔 이유: 사진 인식은 곧 입력 부담을 없애는 기능이라
    아예 막으면 무료 사용자가 손 입력에 지쳐 나가고 구독할 사람 자체가 안 생긴다.
    3회면 한두 끼는 사진으로 처리하면서 차이를 느껴 구독 이유가 생긴다
  - 한도에 걸리면 무료 사용자에게는 구독 안내를, 구독자에게는 한도 안내를 띄운다
    ("음식을 못 알아봤다"와 구분해야 다시 찍는 헛수고를 안 한다)
  - `analyzeDiet`과 분리한 이유: 실패해도 분석 경로에 영향이 없고, 타임아웃도 짧게 잡을 수 있다
  - 이 함수는 b24에서 처음 생겨 호출하는 앱이 전부 토큰을 보내므로 인증을 필수로 요구한다

- **서버 코드 파일 분리** (동작 변화 없음)
  - `index.js` 한 파일이 700줄을 넘겨 어디를 고쳐야 할지 찾기 어려웠다
  - `analyzeDiet` / `extractMeal` / `quota` / `labels` / `gemini` 로 나누고
    `index.js`는 배포 대상만 내보낸다. 배포 명령어는 그대로다
  - 무제한 계정(`unlimited_users`)은 사진 인식도 상한 없이 쓴다 (분석과 동일하게 맞춤)
  - 사용량 확인의 Firestore 왕복을 2회 → 1회로 줄였다 (분석 1회당 약 0.5초 단축).
    무제한 계정 조회를 별도 쿼리가 아니라 같은 트랜잭션 안에서 함께 읽는다
  - 사용량 확인에 3초 상한을 뒀다. 부가 기능이 분석 전체를 붙잡으면 안 된다

- **정산 리마인더 알림** (앱 전용, 서버 불필요)
  - 설정 > 알림에서 켜면 매일 밤 9시에 알려준다. 그날 정산을 끝냈으면 울리지 않는다
  - WorkManager로 기기 안에서만 처리한다 (FCM 불필요)
  - 안드로이드 13+ 알림 권한은 스위치를 켜는 시점에 요청한다. 거부하면 켜지 않는다
    (알림이 안 오는데 켜져 있는 것처럼 보이면 더 혼란스럽다)

- **강제/권장 업데이트 안내 추가** (Firebase Remote Config 기반)
  - 앱을 켤 때 `BuildConfig.VERSION_CODE`와 콘솔의 기준값을 비교한다
    - `min_version_code` 미만 → 닫을 수 없는 강제 안내
    - `recommend_version_code` 미만 → 하루 한 번만 뜨는 권장 안내
    - `force_update_message` / `recommend_update_message` → 다이얼로그 본문
      (비우면 각 상황에 맞는 앱 내장 문구를 쓴다)
  - 기준값을 콘솔에 두는 이유는, 서버 응답 형식을 바꿔야 할 때
    **앱 배포 없이** 구버전 사용자를 차단하기 위해서다
  - 검사에 실패하면(네트워크 없음, 타임아웃, Firebase 장애) 무조건 통과시킨다.
    기본값도 0이라 값을 못 받은 사용자의 앱이 잠기는 일은 없다
  - fetch 대기는 4초, 캐시 유효시간은 1시간 (SDK 기본 12시간은 긴급 대응이 안 됨)
  - ⚠️ b23 이하 사용자에게는 이 다이얼로그가 뜨지 않는다. b24부터 적용된다

- **분석 요청에 `birthDate` 추가** (서버 수정 없이 동작)
  - `functions/index.js`는 이미 `birthDate`로 나이·기초대사량을 계산하고 있었는데
    앱이 이 값을 안 보내서 **모든 사용자가 29세로 계산**되고 있었다
  - 서버는 그대로 두고 앱에서 필드만 채워 보내도록 고쳤다

- **회원탈퇴 최종 확인 창에 탈퇴 대상 계정을 표시**
  - 계정을 여러 개 쓰는 사람이 엉뚱한 계정을 지우는 사고를 막는다
  - 되돌릴 수 없는 마지막 단계에서 이메일을 강조색으로 보여준다

- **로그인 직후 `users/{uid}` 문서를 먼저 만든다** (온보딩 이탈 추적)
  - 예전에는 신체 정보 입력 후 [저장 및 시작]을 눌러야만 문서가 생겼다.
    그래서 로그인만 하고 입력 화면에서 나간 사람은 **Authentication에는 계정이 있는데
    Firestore에는 아무것도 없는** 상태가 되어, 가입자 수도 이탈 지점도 알 수 없었다
  - 이제 프로필이 없으면 `email` / `googleName` / `createdAt` / `profileCompleted=false`를
    먼저 기록하고 입력 팝업을 띄운다
  - `profileCompleted`가 false인 문서 = **가입은 했으나 온보딩 미완료**
  - `createdAt`은 최초 로그인 시각으로 고정된다 (프로필 저장은 merge로 바꿔 덮어쓰지 않는다)
  - 기존 사용자 문서에는 앱을 켤 때 `profileCompleted=true`가 자동으로 채워진다
  - 스텁 쓰기가 실패해도 로그인 흐름은 막지 않는다 (다음 실행 때 보정된다)

- **다국어 지원 (영어 / 한국어)**
  - 앱 화면: 하드코딩된 한글을 모두 문자열 리소스로 뺐다
    - `res/values/strings.xml` = **영어(기본·폴백)**, `res/values-ko/strings.xml` = 한국어
    - 기본을 영어로 둔 이유: 번역이 없는 언어(프랑스어 등)의 기기가 폴백으로 떨어질 때
      한국어보다 영어가 읽을 수 있는 확률이 높다. 한국어 기기는 어차피 `values-ko`를 쓴다
    - ViewModel/Repository는 토스트·예외 문구를 위해 `@ApplicationContext`를 주입받는다
    - `CategoryInfo`는 문자열 대신 `@StringRes` ID를 들고 있도록 바꿨다
  - AI 분석 리포트: 요청에 `language`(BCP-47 태그)를 실어 보내고, 서버가 그 언어로 응답한다
    - 서버의 고정 문구는 `functions/index.js`의 `LABELS` 표에서 언어별로 꺼낸다
    - AI가 쓰는 문장(메뉴명·총평 등)은 프롬프트에 출력 언어를 지시해 처리한다
    - `language`가 없으면(구버전 앱) 한국어로 떨어져 **기존 사용자는 지금과 완전히 동일**하다
    - 지원하지 않는 언어가 오면 한국어가 아니라 영어로 응답한다
  - `locales_config.xml` 추가 → 안드로이드 13 이상에서 [설정 > 앱 > 언어]로 앱 언어만 따로 지정 가능

- **분석 요청에 `appVersion` 추가**
  - 지금 서버는 이 값을 쓰지 않는다 (구조 분해에 없으면 그냥 무시된다)
  - 나중에 서버가 버전별로 응답을 분기하거나 구버전에 안내 문구를 내려주려면
    앱이 먼저 버전을 보내고 있어야 해서 미리 심어둔다

---

## v1.7.5 (b23) — 2026-07-28

### 스토어 문구

```
[개선]
• 정산 화면 맨 위에서 그날의 순칼로리를 바로 볼 수 있습니다.
• 지난 날짜를 열어도 그날 걸음 수가 자동으로 채워집니다. (비어 있을 때만)
• 걸음 수를 다시 불러오는 새로고침 버튼을 추가했습니다.
• 분석 중에 진행 단계와 경과 시간을 보여줍니다.

[수정]
• 분석이 끝난 뒤 결과로 자동 이동하지 않던 문제를 수정했습니다.
• 분석이 정상 처리됐는데도 실패로 표시되던 문제를 수정했습니다.
```

b22와 사용자가 체감하는 차이는 없어 문구는 그대로 둔다.

### 상세 내역

- 가입일시(`createdAt`)를 밀리초 숫자 대신 **Firestore Timestamp**로 저장
  - 콘솔에서 `1785108766791` 대신 날짜/시간으로 보인다
  - 예전 문서는 앱을 한 번 켜면 자동으로 변환된다 (읽을 때는 숫자·Timestamp 둘 다 처리)
  - 이메일 보정과 한 번의 쓰기로 묶어 통신 횟수는 늘지 않았다
  - Room의 `createdAt`은 `Long` 그대로라 스키마 변경 없음

---

## v1.7.4 (b22) — 2026-07-28

### 스토어 문구

```
[개선]
• 정산 화면 맨 위에서 그날의 순칼로리를 바로 볼 수 있습니다.
• 지난 날짜를 열어도 그날 걸음 수가 자동으로 채워집니다. (비어 있을 때만)
• 걸음 수를 다시 불러오는 새로고침 버튼을 추가했습니다.
• 분석 중에 진행 단계와 경과 시간을 보여줍니다.

[수정]
• 분석이 끝난 뒤 결과로 자동 이동하지 않던 문제를 수정했습니다.
• 분석이 정상 처리됐는데도 실패로 표시되던 문제를 수정했습니다.
```

### 상세 내역
- 무제한 계정 등록 방식을 배열에서 **문서 ID = 이메일**로 변경
  - `unlimited_users` 컬렉션에 이메일을 문서 ID로 하는 빈 문서를 만들면 된다
  - 본인 문서만 읽도록 보안 규칙을 걸 수 있어 등록된 이메일 목록이 노출되지 않는다
    (배열은 읽는 순간 다른 사람 이메일까지 다 보였다)
- 사용자 정보에 **구글 로그인 이메일** 저장 추가
  - 기존 사용자는 업데이트 후 앱을 한 번 켜면 자동으로 채워진다 (재로그인 불필요)
  - DB 버전 6 → 7. 마이그레이션(`MIGRATION_6_7`)을 정의해
    기존 정산 기록 캐시가 지워지지 않도록 함
  - 전화번호는 구글 로그인으로 받을 수 없어 제외 (별도 SMS 인증 필요)

---

## v1.7.3 (b21) — 2026-07-27

### 개선
- **정산 화면에서 그날의 순칼로리를 한눈에 볼 수 있도록 변경**
  - 기존에는 순칼로리가 AI 레포트 본문 글 속에만 있어 찾아 읽어야 했음
  - 상단바 날짜 아래에 순칼로리를 함께 표시 (적자는 초록 / 흑자는 빨강)
  - 스크롤 위치나 레포트 길이와 상관없이 항상 보인다
- **지난 날짜도 그날의 걸음 수가 자동으로 채워지도록 변경**
  - 기존에는 오늘 걸음 수만 자동 기입됐음
  - 지난 날짜를 열면 **걸음수가 비어 있을 때만** 그날 걸음 수를 채운다
    (이미 저장된 값이나 직접 입력한 값은 덮어쓰지 않음)
  - 걸음수 새로고침 버튼도 모든 날짜에서 쓸 수 있게 함
  - 30일보다 오래된 날짜도 채울 수 있도록 Health Connect
    "과거 데이터 읽기" 권한(READ_HEALTH_DATA_HISTORY)을 추가로 요청
    (지원하는 기기에서만 요청하며, 거부해도 최근 30일치는 그대로 동작)

### 내부 수정 (사용자에게 보이지 않음)
- 분석 횟수 무제한 계정을 코드 하드코딩에서 **Firestore 조회**로 변경
  - 앱을 새로 배포하지 않아도 대상을 바꿀 수 있다
  - 조회 실패(오프라인) 시 마지막으로 확인된 값 사용
- 결과 카드 자동 스크롤이 동작하지 않던 문제
  - 위치 측정을 AnimatedVisibility 안쪽에서 해 좌표 기준이 어긋나 있었음

---

## v1.7.2 (b20) — 2026-07-27

### 개선
- **AI 분석 중 진행 상황이 한눈에 보이도록 변경**
  - 버튼 안의 작은 스피너 대신 화면 전체를 덮는 안내 팝업 표시
  - 경과 시간(초)과 진행 단계 문구를 함께 보여줌
    (식단 읽는 중 → 칼로리 계산 → 운동·걸음 반영 → 피드백 정리)
  - 분석 중 뒤로가기를 누르면 안내 후 화면에 머무름 (결과를 놓치지 않도록)
- **분석이 끝난 순간을 확실히 알 수 있도록 변경**
  - 완료 시 진동 알림
  - 결과 카드가 나타나면서 그 위치로 자동 스크롤
  - 방금 나온 결과에는 강조 테두리와 "방금 완료" 배지 표시
  - 결과가 이미 있으면 버튼 문구가 "다시 분석하기"로 바뀜

### 수정
- **걸음수가 그날 저장된 기록이 있어야만 표시되던 문제**
  - 걸음 수를 읽어와도 정산 데이터 불러오기가 뒤늦게 끝나면서 0으로 덮어쓰던 문제
  - 이제 데이터 로드가 끝난 뒤 걸음 수를 다시 반영한다
- 걸음수 입력창에 **새로고침 버튼** 추가
  - 직접 입력해 둔 값도 최신 걸음 수로 되돌릴 수 있음
- 분석 요청이 30초 만에 끊겨 정상 분석도 실패로 표시되던 문제
  - 서버 처리 한도(120초)에 맞춰 응답 대기 시간을 늘림

### 내부 수정 (사용자에게 보이지 않음)
- 오늘의 정산 화면 진입 시 데이터 불러오기가 재구성마다 반복 호출되던 문제
  (입력 중인 내용이 초기화될 수 있었음)
- 걸음수 권한 요청 팝업이 중복으로 뜨지 않도록 가드 추가
- 사용하지 않던 분석 결과 스트리밍 코드 정리

---

## v1.7.1 (b19) — 2026-07-23

### 개선
- 뒤로가기가 항상 메인으로 가던 동작을 **직전 화면으로 되돌아가도록** 변경
  - 예: 몸무게 그래프 → 정산 상세 → 뒤로가기 = 몸무게 그래프
  - 화면 이동 기록(back stack)을 쌓아 들어온 경로를 그대로 되짚는다
  - 화면 이름뿐 아니라 **그 화면이 보던 날짜까지 함께 기록**한다.
    (정산 A → 그래프 → 정산 B → 뒤로 두 번 = 정산 A. 예전엔 날짜가 공용 변수라 정산 B가 나왔다)
- 몸무게 그래프에서 정산 상세에 갔다 돌아와도 **보던 스크롤 구간이 유지**되도록 수정

### 내부 수정 (사용자에게 보이지 않음)
- 그래프 화면에서 기록 데이터가 갱신되면 스크롤이 기준 날짜로 되돌아가던 문제
  - 현재 구조에서는 그래프를 보는 동안 기록을 쓸 수 없어 재현되지 않으나,
    이후 백그라운드 동기화가 추가되면 드러날 수 있어 미리 정리
- 설정 화면에서 컴포지션 도중 상태를 변경하던 코드 제거 (재구성 루프 위험)

---

## v1.7.0 (b18) — 2026-07-23

### 신규
- **몸무게 추이 그래프 화면** 추가
  - 정산 입력/상세 화면 우측 상단 그래프 아이콘으로 진입
  - 진입한 화면의 날짜를 기준으로 열리고, 좌우 스와이프로 전체 기간 조회
  - 이전 날짜 대비 몸무게가 바뀐 날만 표시 (같은 값이 이어지는 구간은 생략)
  - 점을 누르면 위에 정보 표시, 한 번 더 누르면 그날 정산으로 이동
- **설정 화면** 추가 (메인 우측 상단 톱니바퀴)
  - 신체 정보(키 / 시작 몸무게 / 성별 / 생년월일) 수정 기능
  - 로그아웃 · 회원탈퇴를 설정 화면으로 이동
  - 회원탈퇴는 전용 화면 + 동의 체크 + 최종 확인까지 3단계로 분리 (오클릭 방지)
- 프로필 시트에 구글 계정 프로필 사진 표시

### 수정
- 설정 / 그래프 화면에서 시스템 뒤로가기 시 앱이 종료되던 문제

---

## v1.6.4 (b17) — 2026-07-23

### 수정
- 최근 추이 차트에서 막대를 눌러 상세로 이동한 뒤 돌아와도
  보던 가로 스크롤 위치가 그대로 유지되도록 수정 (기존에는 맨 끝으로 초기화됨)

---

## v1.6.3 (b16) 이전

이 파일을 만들기 전 버전. 변경 내역은 git 커밋 로그 참고.
